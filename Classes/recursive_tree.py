"""check id the current node is None or if it contains the key
if not, if key is less than current node's key:
      Search the left subtree
    if key greater than current node's key:
      Search the right subtree"""
